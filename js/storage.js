/* gapNinja — this file is no longer used.
   The localStorage-based data layer has been replaced by js/firebase.js, which provides the
   same window.GapNinja.Storage API (resumes/companies/applications/tasks/profile) backed by
   Firestore, scoped per signed-in Google account. Kept here only so old references don't 404;
   not included in index.html. Safe to ignore or delete.
*/
